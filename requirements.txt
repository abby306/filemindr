# Filemindr backend dependencies (Python 3.12).
# Pinned to the versions the test suite runs against; `pip install -r requirements.txt`.

# web framework + server
fastapi==0.138.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# database
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4
pgvector==0.4.2
alembic==1.18.5

# config + validation
pydantic==2.13.4
pydantic-settings==2.14.2

# document processing (OCR routing, parsing)
PyMuPDF==1.27.2.3
python-docx==1.2.0
google-cloud-vision==3.15.0
langdetect==1.0.9

# LLM providers (extraction = DeepSeek via OpenAI-compatible client;
# synthesis = Gemini with GPT-4o escalation)
openai==2.44.0
google-genai==2.10.0

# local embeddings + reranking (bge-base / bge-reranker, CPU)
sentence-transformers==5.6.0
torch==2.12.1

# eval harness
PyYAML==6.0.3

# tests
pytest==9.1.1
httpx==0.28.1
